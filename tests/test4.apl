a0 ← ((+))/1 2 3

⍝ a ← ⍳ 8          ⍝ array [1..8]
⍝ b ← +/ a         ⍝ sum of elements in a 
⍝ f ← {2+⍵×⍵}      ⍝ function x^2 + 2
⍝ c ← +/ f ¨a      ⍝ apply f to all elements of a and sum the elements

⍝ r ← a0 + b + c